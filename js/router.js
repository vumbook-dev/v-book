jQuery(document).ready(function($){

// let boxes = Array.from(document.getElementsByClassName('nav-link'));
// //const state = /
// const title = ["","editor","create-books","book-chapter"];
// // const url = '/';

// boxes.forEach(b => {
//     let id = b.id;
//     //let page = b.data("nav");
//     b.addEventListener('click', e => {
//         history.pushState({id}, `V-Book ${title[id]}`, `./${title[id]}`);
//         $("title").text(`V-Book > ${title[id]}`);
//         //selectBox(id);
//         //console.log("Page Title: "+title[id]+ "id: "+id);
//     });
// });

// history.replaceState(0, '', './');
});